import { Meta } from '@storybook/blocks';
import { defaultThemeColorsArray } from './colorData'

<Meta title="foundations/Theme" />

# Theme Colors

TBD

<div>
  {defaultThemeColorsArray.map(item => (
    <>
      <h2>{item.name}</h2>
      <div class="docs-colorSwatch sb-unstyled theme-light">
        {Object.entries(item.colors).map(([weight, hex]) => {
          const variable = `${item.id}-${weight}`
          return (
            <div class="sb-unstyled theme-light item" key={variable}>
              <div class="color" style={{ backgroundColor: hex }} />
              <div class="info">
                <div>{variable}</div>
                <div class="hex">{hex}</div>
              </div>
            </div>
          )
        })}
      </div>
    </>
  ))}
</div>
