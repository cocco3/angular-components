import { Meta } from '@storybook/blocks';
import { svgIconStore } from './svgIconStore'

<Meta title="foundations/Icons" />

# Icons

Icons sourced from [Untitled UI](https://www.untitledui.com/free-icons).

<div class="container">
  {Object.entries(svgIconStore).map(([key, value]) => (
    <div class="item">
      <div
        class="icon"
        dangerouslySetInnerHTML={{ __html: value }}
      />
      {key}
    </div>
  ))}
</div>

<style>
  {`
  .container {
    columns: 3;
  }

  .item {
    align-items: center;
    display: flex;
    gap: 8px;
    padding-block: 12px;
    padding-inline: 12px;
  }

  .icon {
    display: flex;
    height: 24px;
  }
  `}
</style>
