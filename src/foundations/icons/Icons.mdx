import { Meta } from '@storybook/blocks';
import { svgIconStore } from './svgIconStore'

<Meta title="foundations/Icons" />

# Icons

Icons sourced from [Untitled UI](https://www.untitledui.com/free-icons).

<div style={{
    display: 'grid',
    gap: '8px 16px',
    gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))'
  }}
>
  {Object.entries(svgIconStore).sort().map(([key, value]) => (
    <div
      style={{
        alignItems: 'center',
        background: '#f4f4f4',
        borderRadius: '4px',
        display: 'flex',
        gap: '8px',
        padding: '8px'
      }}
    >
      <div
        style={{ display: 'flex', height: '24px' }}
        dangerouslySetInnerHTML={{ __html: value }}
      />
      {key}
    </div>
  ))}
</div>
